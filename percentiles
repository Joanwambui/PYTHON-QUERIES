import pandas as pd


stories_data["story_date"] = pd.to_datetime(stories_data["story_date"])

data = stories_data.head(20)
print(data)

stories_data.groupby(["user_id","story_date"])["story_count"].sum().quantile([0.25,0.5,0.75])

