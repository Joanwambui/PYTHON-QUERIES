def count_up_to(n):
    count = 1
    while count <= n:
        yield count   # yield pauses the function and returns a value
        count += 1

# Using the generator
counter = count_up_to(5)

for num in counter:
    print(num)
