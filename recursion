def factorial(n):
    print("Factorial called with " + str(n))
    if n < 2:
        print("Returning 1")
        return 1
    else:
        print("Computing", n, "* factorial(", n-1, ")")
        return n * factorial(n - 1)

print(factorial(4))
